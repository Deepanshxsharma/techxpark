import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF2066FF);
  static const Color primaryDark = Color(0xFF174FCC);

  // Backgrounds
  static const Color bgLight = Color(0xFFF5F7FA);
  static const Color bgDark = Color(0xFF0B1220);

  // Cards
  static const Color cardLight = Colors.white;
  static const Color cardDark = Color(0xFF0E1624);

  // Text Colors
  static const Color textDark = Color(0xFF111827);
  static const Color textLight = Color(0xFFE6EEF8);

  // Slot Colors
  static const Color available = Color(0xFF34C759);
  static const Color booked = Color(0xFFFF3B30);

  // Slot Colors Dark Mode
  static const Color availableDark = Color(0xFF2EA84A);
  static const Color bookedDark = Color(0xFFE24A3C);

  // Disabled / Grey
  static const Color grey = Color(0xFF9CA3AF);
}
